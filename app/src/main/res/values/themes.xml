<resources>
    <!-- Base application theme. -->
    <style name="Base.Theme.ZeroSoles" parent="Theme.Material3.Light.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="colorPrimary">@color/light_primary</item>
        <item name="android:textColor">@color/black</item>
    </style>

    <style name="Theme.ZeroSoles" parent="Base.Theme.ZeroSoles" />
</resources>
